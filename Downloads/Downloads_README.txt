This is default directory where files are downloaded.

Downloads directory can be changed by changing the downloads_path in main.py

If you change the path, make sure following directory already exists to prevent errors